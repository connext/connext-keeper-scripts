export type ExtraPropagateParameters = {
  connector: string;
  fee: string;
  encodedData: string;
};

export type ParametersForDomains = {
  connectors: string[];
  encodedData: string[];
  fees: string[];
};
